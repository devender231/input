# Source YAML for BCTL_BILLINGLEVEL
version: 1.0

sources:
  - name: BCTL_BILLINGLEVEL
    source_type: table
    location:
      database: SRC
      schema: SRC
      table: BCTL_BILLINGLEVEL
    
    columns:
      - name: ID
        data_type: INTEGER
        nullable: true
        
      - name: NAME
        data_type: STRING
        nullable: true