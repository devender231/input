# Source YAML for BCTL_STATE
version: 1.0

sources:
  - name: BCTL_STATE
    source_type: table
    location:
      database: SRC
      schema: SRC
      table: BCTL_STATE
    
    columns:
      - name: ID
        data_type: INTEGER
        nullable: true
        
      - name: NAME
        data_type: STRING
        nullable: true