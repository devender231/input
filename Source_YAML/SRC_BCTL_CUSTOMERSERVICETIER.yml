# Source YAML for BCTL_CUSTOMERSERVICETIER
version: 1.0

sources:
  - name: BCTL_CUSTOMERSERVICETIER
    source_type: table
    location:
      database: SRC
      schema: SRC
      table: BCTL_CUSTOMERSERVICETIER
    
    columns:
      - name: ID
        data_type: INTEGER
        nullable: true
        
      - name: NAME
        data_type: STRING
        nullable: true