# Source YAML for BCTL_ACCOUNTTYPE
version: 1.0

sources:
  - name: BCTL_ACCOUNTTYPE
    source_type: table
    location:
      database: SRC
      schema: SRC
      table: BCTL_ACCOUNTTYPE
    
    columns:
      - name: ID
        data_type: INTEGER
        nullable: true
        
      - name: NAME
        data_type: STRING
        nullable: true